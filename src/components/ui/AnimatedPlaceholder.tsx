"use client"; import { motion } from"framer-motion";
import { useEffect, useState } from"react"; export default function AnimatedPlaceholder({ label, imgUrl }: { label: string, imgUrl?: string }) { const [matrix, setMatrix] = useState<string>(""); useEffect(() => { const arr = Array.from({ length: 400 }).map(() => Math.round(Math.random())); setMatrix(arr.join("")); }, []); return ( <div className="relative w-full h-full bg-ink-light overflow-hidden flex items-center justify-center group  rounded-sm"> {imgUrl ? ( <motion.img initial={{ scale: 1.1, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} transition={{ duration: 1.5, ease:"easeOut" }} src={imgUrl} alt={label} className="absolute inset-0 w-full h-full object-cover group-hover:scale-105 transition-transform duration-700 ease-out" /> ) : ( <motion.div animate={{ y: ["0%","-50%"] }} transition={{ repeat: Infinity, duration: 20, ease:"linear" }} className="absolute top-0 left-0 w-full font-mono text-[8px] leading-none opacity-10 text-accent break-all pointer-events-none z-0" > {matrix}{matrix}{matrix}{matrix} </motion.div> )} {!imgUrl && ( <motion.div animate={{ y: ["-100%","200%"] }} transition={{ repeat: Infinity, duration: 3, ease:"linear" }} className="absolute top-0 left-0 w-full h-1 bg-accent/30 blur-sm z-0 pointer-events-none" /> )} {imgUrl && ( <div className="absolute inset-0 bg-gradient-to-t from-ink/80 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 pointer-events-none" /> )} <div className={`relative z-10 font-mono text-[8px] md:text-xs tracking-widest px-2 py-1 md:px-4 md:py-2 transition-colors whitespace-nowrap ${imgUrl ?'text-paper bg-ink/90 absolute bottom-4 left-4 max-w-[90%] overflow-hidden text-ellipsis' :'text-paper/90 bg-ink/80 group-hover:text-accent'}`}> [ {label.toUpperCase()} ] </div> </div> );
}
